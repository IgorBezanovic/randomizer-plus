{
  "name": "randomizer-plus",
  "version": "1.0.8",
  "description": "A library that allows you to randomize everything.",
  "main": "src/index.js",
  "scripts": {
    "clean:types": "rm -rf src/**/*.d.ts && rm -rf src/**/*.d.ts.map && rm -rf src/*.d.ts && rm -rf src/*.d.ts.map",
    "compile": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:watch": "npx jest --watch",
    "lint": "eslint src --ext .js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/IgorBezanovic/randomizer-plus.git"
  },
  "keywords": [
    "randomize"
  ],
  "author": "Igor Bezanovic",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/IgorBezanovic/randomizer-plus/issues"
  },
  "homepage": "https://github.com/IgorBezanovic/randomizer-plus#readme",
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
