/**
 * Generates a welcome message for the Randomizer Plus.
 * @returns {string} A welcome message.
 */
export const randomizeWelcome = (): string => {
    return "Welcome to Randomizer plus"
}
export default randomizeWelcome;